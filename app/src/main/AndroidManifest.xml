<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.my_widget.currencywuzup" >

    <uses-sdk
        android:minSdkVersion="16"
        android:targetSdkVersion="20" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <service
            android:name="com.my_widget.currencywuzup.GetDataService"
            android:permission="android.permission.BIND_REMOTEVIEWS" >
        </service>

        <receiver android:name=".CurrencyWidget" >
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_metadata" >
            </meta-data>
        </receiver>

        <activity
            android:name="com.my_widget.currencywuzup.ConfigActivity"
            android:label="@string/title_activity_config" >
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <service
            android:name="com.my_widget.currencywuzup.PushNotices"
            android:enabled="true"
            android:exported="true" >
        </service>
        <!-- android:process="yo" -->
    </application>

</manifest>
